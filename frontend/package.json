{
  "private": true,
  "dependencies": {
    "aws-sdk": "^2.132.0"
  },
  "devDependencies": {
    "concurrently": "^3.5.0",
    "extract-text-webpack-plugin": "^3.0.2",
    "html-loader": "^0.5.1",
    "html-webpack-plugin": "^2.30.1",
    "js-yaml": "^3.10.0",
    "live-server": "^1.2.0",
    "markdown-loader": "^2.0.1",
    "webpack": "^3.8.1",
    "webpack-cleanup-plugin": "^0.5.1"
  },
  "scripts": {
    "build": "pulp build && webpack",
    "watch": "webpack --watch",
    "browse": "live-server dist/dev --host=localhost --port=3000 --wait=1000 --watch *.js,*.css,*.html --ignore .*",
    "dev": "concurrently \"yarn run watch\" \"yarn run browse\"",
    "deploy": "rake s3:deploy"
  }
}
